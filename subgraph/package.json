{
  "name": "conquest-eth-subgraph",
  "scripts": {
    "generate": "ldenv wait-on ../web/src/lib/contracts.json ~~ node generate.cjs ../contracts/deployments/@@MODE ~~ @@",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:local": "pnpm create:local; pnpm run deploy",
    "deploy:build": "ldenv -m localhost pnpm generate @@MODE ~~ pnpm codegen ~~ pnpm deploy:local ~~ @@",
    "deploy:watch": "ldenv -m localhost as-soon -w src -w templates -w ../contracts/deployments/@@MODE pnpm deploy:build @@MODE @@",
    "schema:watch": "ldenv as-soon -w schema.graphql pnpm codegen",
    "deploy:alchemy": "ldenv graph deploy --version-label v0.0.1 --deploy-key=@@ALCHEMY_GRAPH_TOKEN --ipfs https://ipfs.satsuma.xyz --node https://subgraphs.alchemy.com/api/subgraphs/deploy conquest-eth",
    "create:local": "ldenv wait-on http://@@GRAPH_NODE_GRAPHQL ~~ graph create --node http://@@GRAPH_NODE_API @@SUBGRAPH_NAME ~~",
    "remove:local": "ldenv wait-on http://@@GRAPH_NODE_GRAPHQL ~~ graph remove --node http://@@GRAPH_NODE_API @@SUBGRAPH_NAME ~~",
    "deploy": "ldenv graph deploy --version-label v0.0.1 --node http://@@GRAPH_NODE_API --ipfs @@IPFS_URL @@SUBGRAPH_NAME",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": {
    "as-soon": "^0.0.10",
    "fs-extra": "^11.2.0",
    "handlebars": "^4.7.8",
    "ldenv": "^0.3.11",
    "matchstick-as": "0.5.0",
    "wait-on": "^8.0.1"
  }
}
