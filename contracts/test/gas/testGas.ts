// Gas measurement tests for OuterSpace contract
import {describe} from 'node:test';

describe('GAS: OuterSpace', function () {
	// Gas tests are currently disabled but can be added here
	// Examples:
	// it("get next available virgin planet", async function () {
	//   const {players, outerSpace} = await setupOuterSpace();
	//   const planetStats = outerSpace.findNextAvailablePlanet();
	//   const receipt = await players[0].OuterSpace.write.getPlanetStats([planetStats.location]);
	//   console.log({gasUsed: receipt.gasUsed.toString()});
	// });
});